{
  "name": "agent-launch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "agent-launch": "bin/agent-launch"
  },
  "files": [
    "bin/agent-launch",
    "completions",
    "install.sh",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "matrix:validate": "python docs/tools/agent_matrix/data_utils.py validate",
    "matrix:bundle": "python docs/tools/agent_matrix/data_utils.py bundle",
    "deploy:matrix": "npm run matrix:validate && npm run matrix:bundle && wrangler deploy --config wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.18.0"
  }
}
