{
  "name": "agent-launch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "agent-launch": "bin/agent-launch",
    "agl": "bin/agent-launch"
  },
  "files": [
    "bin/agent-launch",
    "completions",
    "install.sh",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "matrix:validate": "python docs/tools/agent_matrix/data_utils.py validate",
    "matrix:bundle": "python docs/tools/agent_matrix/data_utils.py bundle",
    "matrix:llms-txt": "python docs/tools/agent_matrix/data_utils.py llms-txt",
    "deploy:matrix": "npm run matrix:validate && npm run matrix:bundle && npm run matrix:llms-txt && wrangler deploy --config wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.18.0"
  }
}
